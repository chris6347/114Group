<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">


    <!--!!!!!!!!!!!!!!!!!!!!!注意：：：：不用这个文件!!!!!!!!!!!!!!!!!!!!!!-->



    <!--1. 扫描包 这了不用扫描包了。因为service层已经扫描了。它扫描的是com.itheima-->
    <import resource="applicationContext-service.xml"/>

    <!--2. 注解驱动-->
    <mvc:annotation-driven/>

    <!--3. 静态资源处理 ：笔记上不加这行，
        原因是在web.xml中配置的DispatcherServlet，映射的地址是 *.do4
        如果配置的是 *.do 那么这句话就不需要了。    <mvc:default-servlet-handler/>
        但是要记住，页面发请求的时候，一定要带上  .do 这个尾巴...
    -->
    <mvc:default-servlet-handler/>

    <!--
        4. 视图解析器 这里不需要视图解析器.
            因为在传智健康项目里面，所有的请求从前端过来的，都是异步请求。 ajax请求
            我们只管给请求返回数据即可，返回的是 json数据。
     -->
    <!--<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/"/>
        <property name="suffix" value=".jsp"/>
    </bean>-->

    <!--5. 文件上传组件-->
    <bean id="multipartResolver"
          class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="104857600" />
        <property name="maxInMemorySize" value="4096" />
        <property name="defaultEncoding" value="UTF-8"/>
    </bean>
</beans>